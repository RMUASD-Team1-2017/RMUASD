sudo: required

language: generic

services:
  - docker

script:
  #Build the docker image
- docker-compose build
  #Run tests (only webui for now)
- docker run --rm stefanrvo/rmuasdweb:latest test
- docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
- docker-compose push
